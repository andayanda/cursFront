
function buy(id) {
    console.table(products[id - 1]);
    for (let i = 0-1; i <= products.length-1; i++) {

        if (i ==products[id - 1]) {
            cartList.push(products[id]);
    }
            
       
        cart.push(products[id]);
    }
    console.table(cart)
};
function subtotal(type){
    

}
